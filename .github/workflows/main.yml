name: .NET

on:
  push:
    branches: [ master ]

jobs:
  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3
    #- name: Setup .NET
    #  uses: actions/setup-dotnet@v2
    #  with:
     #   dotnet-version: 6.0.x
    - name: Build
      run: /snap/bin/dotnet build
    - name: Run
      run: /snap/bin/dotnet run
 
